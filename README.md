codewithme
============

# Setup

Get [MongoDB.](http://docs.mongodb.org/manual/installation/)

Download all the dependencies by running:

```shell
$ npm install
```

Run the server:

```shell
$ npm start
```